import {sendPasswordResetEmail} from "https://www.gstatic.com/firebasejs/12.18.0/firebase-auth.js";import {requireUser} from "./guard.js";import {auth} from "./firebase.js";import {toast} from "./ui.js";document.querySelector("#reset-password")?.addEventListener("click",async()=>{const email=auth.currentUser?.email;if(!email)return;await sendPasswordResetEmail(auth,email);toast("Correo de restablecimiento enviado.")});requireUser();
